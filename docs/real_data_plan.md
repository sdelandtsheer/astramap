# Real Data Plan

Milestone 20 will define the adapter path for broker-like or Rubin-like alert data.

The frontend schema should remain stable so synthetic, ZTF-like, broker-derived, or Rubin-like data can be transformed before visualization.

Real-data ingestion is explicitly out of scope for v0.1.
