# Methods

Milestone 16 will document how the synthetic demo data, heuristic scores, and synthetic cutouts are produced.

The current project status is a static-data prototype. It does not use live Rubin Observatory data.

For now, `docs/mvp_scope.md` is the source of truth for the v0.1 scope and caveats.
