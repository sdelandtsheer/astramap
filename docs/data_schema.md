# Data Schema

Milestone 2 will define the exact alert-point and detailed-object schemas. Until then, the draft contract lives in `docs/mvp_scope.md`.

This document will be the contract between the Python data-generation scripts and the React frontend.
